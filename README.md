# purs-lib-template

## Features

- Dev tasks with `just`
- Separate `spago.dhall` and `test.dhall`
  - purescript ide works for both
- `spec` & `spec-discovery`
  - a failing test is added
- Gihub CI
- Nix flake
- LICENSE & CHANGELOG

- `{{author}}`